ALTER TABLE posts ALTER COLUMN status ENUM('DRAFT', 'PENDING_REVIEW', 'PUBLISHED', 'ARCHIVED', 'REJECTED');
ALTER TABLE posts ADD COLUMN IF NOT EXISTS view_count INT DEFAULT 0 NOT NULL;
ALTER TABLE posts ADD COLUMN IF NOT EXISTS publication BOOLEAN DEFAULT FALSE NOT NULL;
ALTER TABLE posts ADD COLUMN IF NOT EXISTS publication_authors VARCHAR(500);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS publication_year INT;
ALTER TABLE posts ADD COLUMN IF NOT EXISTS venue VARCHAR(200);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS abstract_text CLOB;
ALTER TABLE posts ADD COLUMN IF NOT EXISTS keywords VARCHAR(500);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS doi VARCHAR(255);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS bibtex CLOB;
ALTER TABLE posts ADD COLUMN IF NOT EXISTS research_area VARCHAR(150);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS pdf_url VARCHAR(255);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS code_url VARCHAR(255);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS dataset_url VARCHAR(255);
ALTER TABLE posts ADD COLUMN IF NOT EXISTS participant_id VARCHAR(40);
ALTER TABLE users ALTER COLUMN role ENUM('USER', 'CONTRIBUTOR', 'ADMIN');
CREATE TABLE IF NOT EXISTS post_likes (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL,
    post_id BIGINT NOT NULL,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL,
    CONSTRAINT uk_post_likes_user_post UNIQUE (user_id, post_id),
    CONSTRAINT fk_post_likes_user FOREIGN KEY (user_id) REFERENCES users(id),
    CONSTRAINT fk_post_likes_post FOREIGN KEY (post_id) REFERENCES posts(id)
);

CREATE TABLE IF NOT EXISTS user_security_questions (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL,
    question_order INT NOT NULL,
    question_text VARCHAR(255) NOT NULL,
    answer_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL,
    CONSTRAINT uk_user_security_questions_user_order UNIQUE (user_id, question_order),
    CONSTRAINT chk_user_security_questions_order CHECK (question_order BETWEEN 1 AND 3),
    CONSTRAINT fk_user_security_questions_user FOREIGN KEY (user_id) REFERENCES users(id)
);
